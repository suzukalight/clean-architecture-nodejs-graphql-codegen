overwrite: true
schema: 'src/auth/**/*.graphql'
generates:
  src/auth/types.ts:
    plugins:
      - typescript
      - typescript-resolvers
    config:
      useIndexSignature: true
  src/auth/schema.graphql:
    plugins:
      - schema-ast
config:
  scalars:
    DateTime: Date
