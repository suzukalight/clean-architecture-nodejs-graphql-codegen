overwrite: true
schema: 'src/app/**/*.graphql'
generates:
  src/app/types.ts:
    plugins:
      - typescript
      - typescript-resolvers
    config:
      useIndexSignature: true
  src/app/schema.graphql:
    plugins:
      - schema-ast
config:
  scalars:
    DateTime: Date
