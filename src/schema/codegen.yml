overwrite: true
schema: '*/**/*.graphql'
generates:
  types.ts:
    plugins:
      - typescript
      - typescript-resolvers
    config:
      useIndexSignature: true
  schema.graphql:
    plugins:
      - schema-ast
config:
  scalars:
    DateTime: Date
